{
  "$defs": {
    "Arg": {
      "anyOf": [
        {
          "additionalProperties": false,
          "properties": {
            "buf": {
              "type": "string"
            },
            "offset": {
              "format": "uint64",
              "minimum": 0,
              "type": "integer"
            }
          },
          "required": [
            "buf"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "offset": {
              "format": "uint64",
              "minimum": 0,
              "type": "integer"
            },
            "state": {
              "type": "string"
            }
          },
          "required": [
            "state"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "var": {
              "type": "string"
            }
          },
          "required": [
            "var"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "Var-derived integer scalar, e.g. `{\"expr\": {\"mul\": [\"tokens\", 32]}}`.\nSame closed expression set as launch grids; f32 params cannot bind one.",
          "properties": {
            "expr": {
              "$ref": "#/$defs/Expr"
            }
          },
          "required": [
            "expr"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "i32": {
              "format": "int32",
              "type": "integer"
            }
          },
          "required": [
            "i32"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "i64": {
              "format": "int64",
              "type": "integer"
            }
          },
          "required": [
            "i64"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "f32": {
              "format": "float",
              "type": "number"
            }
          },
          "required": [
            "f32"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "u8": {
              "format": "uint8",
              "maximum": 255,
              "minimum": 0,
              "type": "integer"
            }
          },
          "required": [
            "u8"
          ],
          "type": "object"
        }
      ],
      "description": "A call argument. Explicitly tagged so a buffer name can never be\nmistaken for a var name: `{\"buf\": \"hidden\"}`, `{\"state\": \"kv\"}`,\n`{\"var\": \"tokens\"}`, `{\"i32\": 2560}`, `{\"i64\": 4096}`, `{\"f32\": 1e-6}`.\n\nBuffer and state args carry an optional byte `offset` (default 0): the\nkernel receives base + offset. This is how a provider addresses a view\ninside a fused buffer (q/k/v slices of a merged qkv projection) or a\nper-layer region inside an opaque state — the offset is a literal from\nthe provider's own layout arithmetic, the runtime just adds it."
    },
    "Bound": {
      "anyOf": [
        {
          "format": "int64",
          "type": "integer"
        },
        {
          "format": "double",
          "type": "number"
        },
        {
          "$ref": "#/$defs/Expr"
        }
      ],
      "description": "A domain bound: a literal integer, a literal float, or a var expression\n(evaluated at the caller's var values)."
    },
    "Buffer": {
      "additionalProperties": false,
      "properties": {
        "domain": {
          "anyOf": [
            {
              "$ref": "#/$defs/Domain"
            },
            {
              "type": "null"
            }
          ],
          "description": "Optional prior on the buffer's *contents*. Never required: a manifest\nwithout domains runs exactly the same. With one, the runtime rejects\nout-of-domain input writes, and attestation can synthesize valid\nvalues for the buffer (and check produced values against it)."
        },
        "dtype": {
          "$ref": "#/$defs/DType"
        },
        "kind": {
          "$ref": "#/$defs/BufferKind"
        },
        "shape": {
          "items": {
            "$ref": "#/$defs/Dim"
          },
          "type": "array"
        }
      },
      "required": [
        "dtype",
        "shape",
        "kind"
      ],
      "type": "object"
    },
    "BufferKind": {
      "description": "Who provides a buffer and how long its contents live.",
      "oneOf": [
        {
          "const": "input",
          "description": "Written by the runtime before program execution.",
          "type": "string"
        },
        {
          "const": "output",
          "description": "Read back by the runtime after program execution.",
          "type": "string"
        },
        {
          "const": "weight",
          "description": "Bound by name from the weight artifact at load time.",
          "type": "string"
        },
        {
          "const": "workspace",
          "description": "Planned and owned by the runtime; contents dead across program runs.",
          "type": "string"
        },
        {
          "const": "carry",
          "description": "Written by one program, read by another; contents persist across\nprogram runs. Which program runs first is the caller's contract —\nthe verifier only requires that *some* program writes it.",
          "type": "string"
        }
      ]
    },
    "Call": {
      "additionalProperties": false,
      "description": "A call site: which op, with which args. No geometry — grid/block belong\nto the op's implementation. `label` is for humans and diagnostics only.",
      "properties": {
        "args": {
          "items": {
            "$ref": "#/$defs/Arg"
          },
          "type": "array"
        },
        "label": {
          "type": [
            "string",
            "null"
          ]
        },
        "op": {
          "type": "string"
        }
      },
      "required": [
        "op",
        "args"
      ],
      "type": "object"
    },
    "DType": {
      "description": "Element type of a buffer or scratch declaration.",
      "enum": [
        "bf16",
        "f16",
        "f32",
        "fp8e4m3",
        "i32",
        "u32",
        "i64",
        "u8"
      ],
      "type": "string"
    },
    "Dim": {
      "anyOf": [
        {
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        {
          "type": "string"
        }
      ],
      "description": "One shape extent: a constant or the name of a var."
    },
    "Domain": {
      "additionalProperties": false,
      "description": "A prior on buffer contents, declared by whoever wires the model — the\nonly party that knows a `buffer<i32>` is a page table and not an\nactivation. It is unary (one buffer, no relations between buffers) and\nsays nothing about kernel behaviour: a kernel that misbehaves on\nin-domain input is a kernel bug the harness can now provoke.\n\nTwo forms, mutually exclusive:\n- `min`/`max`: inclusive bounds, integers, floats or a var expression;\n  either may be omitted. Float buffers without a domain are implicitly\n  \"any finite value\".\n- `index_into`: every element indexes a row of the named buffer or a\n  token slot of the named state, `stride` rows/tokens per index (a paged\n  KV block table indexes 16 tokens at a time).\n\n`monotone` additionally requires a non-decreasing sequence (prefix sums\nsuch as `cu_seqlens`).",
      "properties": {
        "index_into": {
          "type": [
            "string",
            "null"
          ]
        },
        "max": {
          "anyOf": [
            {
              "$ref": "#/$defs/Bound"
            },
            {
              "type": "null"
            }
          ]
        },
        "min": {
          "anyOf": [
            {
              "$ref": "#/$defs/Bound"
            },
            {
              "type": "null"
            }
          ]
        },
        "monotone": {
          "type": "boolean"
        },
        "stride": {
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "Expr": {
      "anyOf": [
        {
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        {
          "description": "A var by name — the same bare-string form a shape uses.",
          "type": "string"
        },
        {
          "properties": {
            "ceil_div": {
              "maxItems": 2,
              "minItems": 2,
              "prefixItems": [
                {
                  "$ref": "#/$defs/Expr"
                },
                {
                  "format": "uint64",
                  "minimum": 0,
                  "type": "integer"
                }
              ],
              "type": "array"
            }
          },
          "required": [
            "ceil_div"
          ],
          "type": "object"
        },
        {
          "properties": {
            "mul": {
              "maxItems": 2,
              "minItems": 2,
              "prefixItems": [
                {
                  "$ref": "#/$defs/Expr"
                },
                {
                  "format": "uint64",
                  "minimum": 0,
                  "type": "integer"
                }
              ],
              "type": "array"
            }
          },
          "required": [
            "mul"
          ],
          "type": "object"
        }
      ],
      "description": "The closed scalar-expression set: a constant, a var name (`\"tokens\"`),\n`{\"ceil_div\": [e, c]}`, `{\"mul\": [e, c]}`. This is deliberately not a\nlanguage: grid geometry, dynamic shared memory and var-derived scalar\nargs are the only runtime-dependent numbers a provider may compute, and\nonly with these forms."
    },
    "Impl": {
      "additionalProperties": false,
      "description": "An implementation is a micro-program: private scratch buffers (sized by\nvar expressions, provisioned by the runtime, dead outside one call) and\none or more launches. Launch geometry lives here, not at the call site —\nit belongs to the implementation.",
      "properties": {
        "launches": {
          "items": {
            "$ref": "#/$defs/Launch"
          },
          "type": "array"
        },
        "scratch": {
          "additionalProperties": {
            "$ref": "#/$defs/Scratch"
          },
          "type": "object"
        }
      },
      "required": [
        "launches"
      ],
      "type": "object"
    },
    "Launch": {
      "additionalProperties": false,
      "description": "One launch inside an implementation. The common single-launch op whose\nABI *is* the interface writes only `module`, `entry`, `block`, `grid`:\n`params` defaults to the op's params and `args` to forwarding them in\norder.",
      "properties": {
        "args": {
          "description": "Wiring: where each launch param comes from — a forwarded interface\nparam, a scratch buffer, or an implementation-private literal.\nAbsent: `[{\"param\": 0}, {\"param\": 1}, ...]` over the op's params.",
          "items": {
            "$ref": "#/$defs/LaunchArg"
          },
          "type": [
            "array",
            "null"
          ]
        },
        "block": {
          "description": "Required unless `entry` is `extern:`.",
          "items": {
            "format": "uint32",
            "minimum": 0,
            "type": "integer"
          },
          "maxItems": 3,
          "minItems": 3,
          "type": [
            "array",
            "null"
          ]
        },
        "entry": {
          "description": "Entry point in the module, or `extern:<name>` for a runtime built-in\n(no module, no geometry).",
          "type": "string"
        },
        "grid": {
          "items": {
            "$ref": "#/$defs/Expr"
          },
          "maxItems": 3,
          "minItems": 3,
          "type": [
            "array",
            "null"
          ]
        },
        "module": {
          "description": "Name of the `modules` entry this entry point lives in. Absent: the\nruntime searches every loaded module (disambiguating by param\nlayout) — mined kernels that only know their symbol. `extern:`\nentries have no module.",
          "type": [
            "string",
            "null"
          ]
        },
        "params": {
          "description": "This launch's own ABI (what the entry actually takes). Absent: the\nop's `params`.",
          "items": {
            "$ref": "#/$defs/ParamType"
          },
          "type": [
            "array",
            "null"
          ]
        },
        "shared_mem": {
          "anyOf": [
            {
              "$ref": "#/$defs/Expr"
            },
            {
              "type": "null"
            }
          ],
          "description": "Dynamic shared memory in bytes, if the launch needs any."
        }
      },
      "required": [
        "entry"
      ],
      "type": "object"
    },
    "LaunchArg": {
      "anyOf": [
        {
          "additionalProperties": false,
          "properties": {
            "param": {
              "format": "uint",
              "minimum": 0,
              "type": "integer"
            }
          },
          "required": [
            "param"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "scratch": {
              "type": "string"
            }
          },
          "required": [
            "scratch"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "i32": {
              "format": "int32",
              "type": "integer"
            }
          },
          "required": [
            "i32"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "i64": {
              "format": "int64",
              "type": "integer"
            }
          },
          "required": [
            "i64"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "f32": {
              "format": "float",
              "type": "number"
            }
          },
          "required": [
            "f32"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "u8": {
              "format": "uint8",
              "maximum": 255,
              "minimum": 0,
              "type": "integer"
            }
          },
          "required": [
            "u8"
          ],
          "type": "object"
        }
      ],
      "description": "A launch argument. `{\"param\": 0}` forwards the call's arg #0 verbatim;\n`{\"scratch\": \"pmax\"}` passes a private scratch pointer; scalar literals\nare implementation constants the interface never sees (a partial-count\nbaked into a two-stage reduction, a mined ABI's strides and flags)."
    },
    "Module": {
      "additionalProperties": false,
      "description": "A code artifact the launches pin: a local file name (`argmax.cubin`) or a\nregistry ref (`hf:org/repo/path[@rev]`). The runtime never resolves by\n`source`: it loads every artifact in the kernel dir (plus fetched\nregistry refs), hashes each, and matches by `sha256` — so one dir can hold\nevery version of a kernel, and a rebuilt artifact with different bytes\nis a different module. `source` is a label and, for registry refs, a URL.",
      "properties": {
        "sha256": {
          "type": "string"
        },
        "source": {
          "type": "string"
        }
      },
      "required": [
        "source",
        "sha256"
      ],
      "type": "object"
    },
    "Op": {
      "additionalProperties": false,
      "description": "An op is an **interface** (the typed params a call passes — the only\nthing a call site knows) plus an **implementation**: how those args are\nlowered onto actual launches. Swapping the implementation — a faster\nkernel from elsewhere with the same interface — touches only `impl`;\nevery call stays untouched.",
      "properties": {
        "impl": {
          "$ref": "#/$defs/Impl"
        },
        "params": {
          "description": "The interface: typed, directional params. Call-site semantics\n(what each position means) are the contract implementations must\nhonor; the verifier can only check shape, not meaning.",
          "items": {
            "$ref": "#/$defs/ParamType"
          },
          "type": "array"
        }
      },
      "required": [
        "params",
        "impl"
      ],
      "type": "object"
    },
    "ParamType": {
      "description": "One parameter: a by-value scalar (`\"i32\"`, `\"i64\"`, `\"f32\"`, `\"u8\"`) or a directional pointer — `\"in\"`/`\"out\"`/`\"inout\"` followed by `\"state\"` (opaque state base) or `\"buffer<dtype>\"`.",
      "pattern": "^(i32|i64|f32|u8|(in|out|inout) (state|buffer<(bf16|f16|f32|fp8e4m3|i32|u32|i64|u8)>))$",
      "type": "string"
    },
    "Scratch": {
      "additionalProperties": false,
      "description": "One scratch buffer declaration: like a workspace buffer, but private to\nthe implementation.",
      "properties": {
        "dtype": {
          "$ref": "#/$defs/DType"
        },
        "shape": {
          "items": {
            "$ref": "#/$defs/Dim"
          },
          "type": "array"
        }
      },
      "required": [
        "dtype",
        "shape"
      ],
      "type": "object"
    },
    "Spec": {
      "additionalProperties": false,
      "description": "How a driver stages the `draft` / `verify` programs of a speculative\nmanifest: `draft` runs over `block` rows = the anchor token followed by\n`block - 1` copies of `mask_token`, `verify` over `block` rows = the\nanchor followed by the `block - 1` drafted tokens.",
      "properties": {
        "block": {
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "mask_token": {
          "format": "int64",
          "type": "integer"
        }
      },
      "required": [
        "block",
        "mask_token"
      ],
      "type": "object"
    },
    "State": {
      "additionalProperties": false,
      "description": "Opaque persistent memory. The runtime knows only how many bytes to\nprovision — per token slot (`bytes_per_token`, scaled by the capacity:\npaged KV) or one fixed block regardless of capacity (`bytes`:\nper-sequence recurrent state such as a Mamba/GDN conv + SSM state).\nExactly one of the two is non-zero. The internal layout belongs to the\nprovider's kernels, which receive the base pointer as a `state` param.",
      "properties": {
        "bytes": {
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "bytes_per_token": {
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "Var": {
      "additionalProperties": false,
      "description": "A per-call scalar the caller supplies (e.g. the token count this step),\nbounded `1..=max`. Vars are the only thing that may size a shape or a\nlaunch grid; any other caller-supplied number is data and goes through an\ninput buffer. Verification runs at the bounds; the runtime rejects an\nout-of-range value at call time.",
      "properties": {
        "max": {
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "max"
      ],
      "type": "object"
    }
  },
  "$id": "https://kern-baa.pages.dev/schema/manifest-v3.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "buffers": {
      "additionalProperties": {
        "$ref": "#/$defs/Buffer"
      },
      "type": "object"
    },
    "model": {
      "description": "Free-form label; the runtime assigns no meaning to it.",
      "type": "string"
    },
    "modules": {
      "additionalProperties": {
        "$ref": "#/$defs/Module"
      },
      "type": "object"
    },
    "ops": {
      "additionalProperties": {
        "$ref": "#/$defs/Op"
      },
      "type": "object"
    },
    "programs": {
      "additionalProperties": {
        "items": {
          "$ref": "#/$defs/Call"
        },
        "type": "array"
      },
      "description": "Each program is a straight-line list of op calls.",
      "type": "object"
    },
    "schema_version": {
      "description": "Wire-format version. Only [`SCHEMA_VERSION`] is accepted.",
      "format": "uint32",
      "minimum": 0,
      "type": "integer"
    },
    "spec": {
      "anyOf": [
        {
          "$ref": "#/$defs/Spec"
        },
        {
          "type": "null"
        }
      ],
      "description": "Caller contract of a speculative-decoding manifest (absent for plain\nprefill/decode ones). The runtime assigns no meaning to it."
    },
    "states": {
      "additionalProperties": {
        "$ref": "#/$defs/State"
      },
      "type": "object"
    },
    "vars": {
      "additionalProperties": {
        "$ref": "#/$defs/Var"
      },
      "type": "object"
    }
  },
  "required": [
    "schema_version",
    "model",
    "buffers",
    "ops",
    "programs"
  ],
  "title": "kern manifest v3",
  "type": "object"
}
